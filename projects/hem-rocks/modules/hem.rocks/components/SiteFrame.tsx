import React, { PropsWithChildren, ReactElement } from 'react'

interface IProps {}

function SiteFrame({}: PropsWithChildren<IProps>): ReactElement {
  return (
    <div className="hem-site-frame">
    </div>
  )
}

export default SiteFrame
