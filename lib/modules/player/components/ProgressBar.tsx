import React, { ReactElement } from 'react'

interface IProps {

}

function ProgressBar({ }: IProps): ReactElement {
  return (
    <button className="hem-player-progress-bar" />
  )
}

export default ProgressBar
