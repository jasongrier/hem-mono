// TODO: Type and name should come from the .env file.
// TODO: Whether to launch in browser or in electrong should come from the .env file.
window.env = {
  PROJECT_TYPE: 'sites', // `apps` or `sites`
  PROJECT_NAME: 'sl',
}
