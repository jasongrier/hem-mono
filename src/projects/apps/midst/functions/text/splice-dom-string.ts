/**
 * Splice content into subject at a given "real" index, ignoring HTML tags.
 * (Only the HTML tags that are used in the app...)
 * @param index
 * @param content
 */
function spliceDomString(index: number, subject: string, content: string) {
  for (let i = 0; i < subject.length; i++) {

  }
}

export default spliceDomString
